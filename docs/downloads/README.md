# StreamVault Downloads

Place the Windows installer here only if you are using Git LFS:

```text
StreamVault-Setup-1.5.0.exe
```

The website already links to that filename.

If you are not using Git LFS, upload the installer to GitHub Releases instead and update the download links in `docs/index.html` to the release asset URL.
